# Demo Catalog Azure - Values
# Minimal values for static RGD catalog

nameOverride: ""
fullnameOverride: ""

# --- Dependencies ---
# Enable to install Azure Service Operator alongside the RGDs
azure-service-operator:
  enabled: false

# Enable to install Kro alongside the RGDs
kro:
  enabled: false
